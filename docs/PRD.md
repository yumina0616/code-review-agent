# PRD — Code Review & Refactoring Agent

> `[TODO]`를 직접 채우세요. 이번 프로젝트는 도메인 지식이 필요 없으니, PRD/TRD 작성 자체(Spec Driven Development 연습)에 집중하면 됩니다.

## 1. 배경
`src/mini_inventory/inventory.py`는 동작은 하지만 품질이 낮은 코드다 (전역 상태, I/O와 로직 뒤섞임, 타입힌트 없음 등). 이런 코드는 실제 랩실 프로젝트에서도 급하게 짠 프로토타입 코드가 방치되는 형태로 흔히 등장한다.

## 2. 목표
- **재사용성 (확정)**: `code-reviewer`/`refactorer` subagent는 이번 프로젝트 전용이 아니라 **다른 파이썬 프로젝트에도 재사용 가능한 템플릿**으로 설계한다.
  - 측정 기준: subagent 정의 파일(`.claude/agents/*.md`)이 `mini_inventory`라는 프로젝트명이나 `inventory.py` 같은 특정 파일 경로를 하드코딩하지 않는다. CLAUDE.md만 다른 프로젝트 것으로 바꿔 끼우면 동일한 subagent가 그대로 동작해야 한다.
  - (참고: 지금 작성된 두 subagent 파일은 이미 이 조건을 만족함 — CLAUDE.md 참조만 하고 프로젝트 고유명사가 없음. 앞으로 수정할 때도 이 조건 깨지 않도록 주의)
- **성공의 정량 기준**: CLAUDE.md의 8개 품질 기준 100% 충족
- [TODO] 8개 기준 중 어떤 게 제일 어려울 것 같은지 예측해서 한 줄 적어보기 — 힌트: mypy --strict는 보통 처음에 에러가 제일 많이 나옵니다. 왜 그럴지 생각해보고 8번 리스크 섹션에 연결해보세요

## 3. Non-goals
- **CLI에 새 기능(파일 저장/불러오기 등) 추가는 이번 스코프에서 명시적으로 제외한다 (확정).** 순수 리팩토링(구조 개선)만 다룬다 — 기능 추가는 CLAUDE.md 1번 규칙("동작은 그대로 유지")과도 상충되므로 애초에 겸할 수 없음
- 성능 최적화는 다루지 않음

## 4. 사용자 시나리오
- [TODO] "나는 이 코드를 유지보수해야 하는 팀원으로서, 전역 상태 때문에 테스트를 못 짜서 답답하다" 같은 형태로 2~3개 작성

## 5. 기능 요구사항
- code-reviewer와 refactorer 두 서브에이전트의 입출력 흐름은 `.claude/commands/goal.md`에 이미 초안이 있음 — [TODO] 그걸 읽고 부족한 부분(예: refactorer가 code-reviewer의 리포트를 정확히 어떤 형식으로 받는지)이 있으면 채우기
- **characterization test 작성 주체 (확정): refactorer가 스스로 작성한다.**
  - 이유: 사람이 매번 먼저 써야 한다면, 목표 2번의 "재사용 템플릿" 요건과 상충됨 (새 프로젝트마다 사람 개입이 필요해지므로 "템플릿"이라 부르기 어려워짐)
  - 트레이드오프: refactorer가 스스로 만든 테스트가 부실하면(예: 몇 개 케이스만 대충 커버) 리팩토링이 실제로 동작을 몰래 바꿔도 못 잡아낼 수 있음 — 이건 8번 리스크에 정리

## 6. 비기능 요구사항
- CLAUDE.md의 8개 기준을 그대로 참조

## 7. 완료 조건 요약
- [TODO] `/goal`에 그대로 들어갈 문장 3~5개. CLAUDE.md 8개 기준을 요약해도 되고, 우선순위를 다르게 매겨도 됨

## 8. 리스크 / 열린 질문
- [TODO] "동작을 그대로 유지하면서 구조를 바꾼다"는 게 실제로 얼마나 어려울지 — characterization test가 허술하면 무슨 일이 생길지 생각해보기
- [TODO] mypy --strict가 처음엔 대량의 에러를 쏟아낼 텐데, 이걸 자기수정 루프가 5회 안에 다 해결 못하면 어떻게 할지
- **재사용성 vs 구체성 트레이드오프 (2번 목표 결정에서 파생됨)**: subagent 프롬프트를 너무 일반적으로 쓰면(어느 프로젝트에나 맞게) 이번 `mini_inventory` 프로젝트 특유의 문제(예: 전역 상태 `data = {}`)를 콕 집어 지적하는 능력이 떨어질 수 있음. 반대로 너무 구체적으로 쓰면 재사용이 안 됨. [TODO] 지금 작성된 두 subagent 파일이 이 균형을 잘 잡았는지, 실제로 `/goal` 돌려보고 판단해서 여기에 결론 적기